<%- include('partials/header') %>

<h2>Here are some things that we sell:</h2>
<ul class="pill-list">
  <% for (let i = 0; i < productCategories.length; i++) { %>
  <li class="pill"><%= productCategories[i] %></li>
  <% } %>
</ul>

<%- include('partials/footer') %>
