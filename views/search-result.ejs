<%- include('partials/header') %>

<h2>Search Results</h2>

<% if (search.search_text && search.category) { %>
<p>
  You searched for "<strong><%= search.search_text %></strong>" in "<strong
    ><%= search.category %></strong
  >".
</p>
<p><em>(Demo app – no real DB yet.)</em></p>
<% } else { %>
<p>No search parameters provided.</p>
<% } %> <%- include('partials/footer') %>
